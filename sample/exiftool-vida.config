# TBD: Have this function do signing when writing, and validation when reading.
%Image::ExifTool::UserDefined = (
    'Image::ExifTool::XMP::xmp' => {
        # vida tag (simple string, no checking)
        vida => { },
    },
);
1;
